#include <stdio.h>
#include <stdlib.h>

int graph[8][8];

void print_graph(int f_size, int s_size) {
  for (int i = 0; i <= f_size; i++) {
    for (int j = 0; j <= s_size; j++) {
      printf("%d ", dp[i][j]);
    }
    printf("\n");
  }
}

int main(void) {
  int input

      solution(first, second);
  return 0;
}